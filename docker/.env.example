# PostgreSQL
POSTGRES_USER=matei
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=matei

# Encryption key for AES-256-GCM credential encryption (32 bytes hex-encoded)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Backend
NODE_ENV=development
API_PORT=3001
COLLECTOR_INTERVAL_MS=30000
COLLECTOR_WORKERS=40
